public class Solution {
    public int gcd(int A, int B) {
        if(A==0)
         return B;
        if(B==0)
         return A;
        while(A!=B)
        {
        if(A>B)
         A-=B;
        else
         B-=A;
        }
        return B;
    }
}
