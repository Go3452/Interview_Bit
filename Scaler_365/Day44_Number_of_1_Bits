public class Solution {
	public int numSetBits(long a) {
        int count=0;
        while(a>0)
        {
         long r = a%2;
         if(r==1)
          count++;
          a/=2;
        }
        return count;
	}
}
