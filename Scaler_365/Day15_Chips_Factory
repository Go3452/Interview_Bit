public class Solution {
    public int[] solve(int[] A) {
        int []arr = new int[A.length];
        for(int i=0,j=0;i<A.length;i++)
        {
         if(A[i]!=0)
          { arr[j]=A[i];
            j++;
          }
        }
        return arr;
    }
}
