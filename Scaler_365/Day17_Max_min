public class Solution {
    public int solve(int[] A) {
        int min=A[0],max=A[0];
        for(int i=1;i<A.length;i++)
        {
         if(min>A[i])
          min=A[i];
         else if(max<A[i])
          max=A[i];
        }
        return max+min;
    }
}
