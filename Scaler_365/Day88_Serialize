public class Solution {
    public String serialize(String[] A) {
        String ans="";
        for(int i=0;i<A.length;i++)
        {
         String s=A[i];
         int n=s.length();
         ans+=s+Integer.toString(n)+'~';
        }
        return ans;
    }
}
